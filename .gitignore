.build/
.swiftpm/
*.xcodeproj/
*.xcworkspace/
DerivedData/
.DS_Store
dist/

# Secrets are injected by CI — keep the template, not real values
# If you edit Secrets.swift locally, don't commit it
# Sources/SlackMentionNotifier/Secrets.swift
